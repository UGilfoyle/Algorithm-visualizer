{
  "name": "algorithm-visualizer",
  "version": "1.0.0",
  "description": "Algorithm visualization platform with visitor tracking",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "start": "vercel dev",
    "init-db": "node api/init-db.js"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
